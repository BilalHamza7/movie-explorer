import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#799BB9',
      light: '#94AFC7',
      dark: '#547DA0',
      contrastText: '#242105',
    },
  },
});

export default theme;